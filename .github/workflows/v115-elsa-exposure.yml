name: v115 patch

on:
  push:
    paths:
      - ".github/workflows/v115-elsa-exposure.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow ok"
